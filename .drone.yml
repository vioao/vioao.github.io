kind: pipeline
name: default

steps:
  - name: build
    image: node
    volumes:
      - name: target
        path: /drone/src/public
    commands:
      - npm install
      - ./node_modules/hexo/bin/hexo clean
      - ./node_modules/hexo/bin/hexo g
      - cd /drone/src/public && ll
    when:
      branch:
        - blog

volumes:
  - name: target
    host:
      path: /var/www/mine